#include "../include/GenerateWave.hpp"
#include <cmath>

AdvWaveGenerator::AdvWaveGenerator(int grid_var, float boxSize) : int m_grid_var(grid_var), float m_boxSize(boxSize), float
std::vector<electricFieldPoint> AdvWaveGenerator::AdvGenerateCoordinates(){
  float center.x = float center.y = float center.z = boxSize/2;
  

  float const k = 8.987 * 10^9;
  float const Q = 1.0e-6f;
  float value = (c*x/)
}
